function add(x, y){
    return x + y;
}

function subtract(x, y){
    return x - y;
}

function multiply(x, y){
    return x * y;
}

function divide(x, y){
    return x / y;
}

let a = Number(prompt("Enter a number to add."));
let b = Number(prompt('Enter another one.'));

alert(divide(a, b));